spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/booking-schedule-db
    username: auth_service
    password: 09022005

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true

server:
  port: 8083
